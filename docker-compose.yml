services:
  app:
    image: ghcr.io/vocalbeam/ekron-outbound:latest
    container_name: ekron-outbound
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    env_file:
      - .env
    restart: unless-stopped
